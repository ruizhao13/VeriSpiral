# Public workflow problem

Find a path of minimum total integer weight from node 0 to the last node in
the supplied small directed acyclic graph. Negative weights are allowed.
The answer must use the original graph and carry a checkable optimality
certificate. This purpose-built public problem has no real-world route claim.
